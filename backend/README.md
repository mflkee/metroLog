# Backend

FastAPI backend skeleton for metroLog.

## Quick start

```bash
env UV_CACHE_DIR=/tmp/uv-cache uv sync --dev
env UV_CACHE_DIR=/tmp/uv-cache uv run uvicorn app.main:app --reload
```

## Tests

```bash
env UV_CACHE_DIR=/tmp/uv-cache uv run pytest
```

